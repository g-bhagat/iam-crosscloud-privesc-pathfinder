# Collectors (task 7-8)
boto3>=1.34
google-cloud-asset>=4.0
google-auth>=2.30
# The IAM Admin API v1's Workload Identity Pool/Provider admin surface and
# per-SA getIamPolicy aren't covered by the google-cloud-iam gRPC client --
# GCPCollector uses the REST discovery client for those instead.
google-api-python-client>=2.100

# Analysis layer (task 9-11)
networkx>=3.2

# Visualization export (task 12)
pyvis>=0.3.2

# Tests
pytest>=8.0
moto[iam,sts]>=5.0
